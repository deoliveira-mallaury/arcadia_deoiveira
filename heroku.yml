build:
  docker:
    web: Dockerfile
    worker: docker/build/nginx/nginx.Dockerfile
run:
  web: php -S 0.0.0.0:$PORT -t public/
  worker: nginx -g 'daemon off;'
build:
  docker:
    web: Dockerfile
    worker: worker/Dockerfile